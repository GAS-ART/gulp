Может кто то еще подсказать как правильно писать console.log() в галпе?

к примеру я хочу проверить что в переменных
// Выгружаем в папку с результатом
      .pipe(app.gulp.dest(`${app.path.build.fonts}`))
      // Ищем файлы шрифтов .ttf
      .pipe(app.gulp.src(`${app.path.srcFolder}/fonts/*.ttf`))

Допустим у меня какая то ошибка именно в пути или переменная выдает не те данные что нужно

По шрифтам был неправильны ключ в пути, промудохался 2 часа пока нашел ош